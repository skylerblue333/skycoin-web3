{
  "name": "@skycoin4444/web3-credential-metadata",
  "version": "1.1.0-beta.1",
  "description": "Deterministic Web3 credential metadata validation primitives",
  "main": "dist/credentials.js",
  "types": "dist/credentials.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node tests/credentials.test.js",
    "audit": "npm audit --omit=dev --audit-level=high"
  },
  "engines": { "node": ">=20" },
  "keywords": ["skycoin4444", "web3", "credentials", "metadata"],
  "author": "SKYCOIN4444",
  "license": "MIT",
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
